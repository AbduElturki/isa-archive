apiVersion: isa-archive/v1
kind: ISA
metadata:
  name: rv32im
  description: RV32I + M (integer multiply/divide) extension
spec:
  version: "2.1"
  extends: "../base/isa.yaml"
  includes:
    - "instructions.yaml"
