apiVersion: isa-archive/v1
kind: ISA
metadata:
  name: pico32m
  description: pico32 + hardware multiply — an extension, not a fork.
spec:
  version: "0.4"
  extends: "../isa.yaml"
  includes:
    - "instructions.yaml"
