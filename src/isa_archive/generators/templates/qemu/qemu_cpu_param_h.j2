// This code was generated by ISA-Archive.
#pragma once
{%- if xlen != tcg_bits %}
/* Architectural data width is {{ xlen }} bits, emulated over a {{ tcg_bits }}-bit
 * guest word (TCG has only 32/64-bit targets); PC and addresses are masked
 * to {{ xlen }} bits by the generated helpers. */
{%- endif %}
#define TARGET_LONG_BITS              {{ tcg_bits }}
#define TARGET_PAGE_BITS              {{ page_bits }}
#define TARGET_PHYS_ADDR_SPACE_BITS   {{ addr_bits }}
#define TARGET_VIRT_ADDR_SPACE_BITS   {{ addr_bits }}
#define TARGET_INSN_START_WORDS       1
