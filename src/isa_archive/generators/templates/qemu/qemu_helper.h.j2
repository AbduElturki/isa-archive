// This code was generated by ISA-Archive. The tool is provided "as is", without warranty of any kind. Full ownership of the generated code belongs to the user or organization.
// No include guard — this file is designed to be processed multiple times
// with different DEF_HELPER_FLAGS_N macro definitions.

DEF_HELPER_FLAGS_2(raise_exception, TCG_CALL_NO_WG, noreturn, env, i32)

{% for instr in instructions.values() %}
{%- set info = instr | qemu_info(isa_reg) -%}
DEF_HELPER_FLAGS_{{ info.helper_args | length + 1 }}({{ instr.metadata.name | lower | replace("-", "_") }}, 0, void, env{% for arg in info.helper_args %}, {{ tcg_type }}{% endfor %})
{% endfor %}
