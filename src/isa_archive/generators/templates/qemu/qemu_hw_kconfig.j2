# Generated by ISA-Archive.
config {{ isa_name | upper | replace("-", "_") }}_VIRT
    bool
    default y
    depends on {{ isa_name | upper | replace("-", "_") }}
{%- for p in (machine.qemu.devices if machine.qemu else []) %}
{%- if p.type == "ns16550" %}
    imply SERIAL_MM
{%- elif p.type == "sifive_test" %}
    imply SIFIVE_TEST
{%- endif %}
{%- endfor %}
