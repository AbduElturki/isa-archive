// {{ isa_name }} TargetInfo — generated by isa-archive

#include "TargetInfo/{{ ISA }}TargetInfo.h"
#include "llvm/MC/TargetRegistry.h"

using namespace llvm;

Target &llvm::getThe{{ ISA }}Target() {
  static Target The{{ ISA }}Target;
  return The{{ ISA }}Target;
}

extern "C" LLVM_EXTERNAL_VISIBILITY void LLVMInitialize{{ ISA }}TargetInfo() {
  RegisterTarget<Triple::{{ triple_arch }}, /*HasJIT=*/false> X(
      getThe{{ ISA }}Target(), "{{ isa_name }}", "{{ isa_name }} ({{ xlen }}-bit)", "{{ ISA }}");
}
