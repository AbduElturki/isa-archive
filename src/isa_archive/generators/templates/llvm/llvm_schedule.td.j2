// {{ isa_name }} Scheduling model — generated by isa-archive
// Basic in-order model with unit latencies.

def {{ ISA }}SchedModel : SchedMachineModel {
  let IssueWidth = 1;
  let MicroOpBufferSize = 0;  // in-order
  let LoadLatency = 2;
  let MispredictPenalty = 3;
  let CompleteModel = 0;
}

def {{ ISA }}WriteALU  : SchedWrite;
def {{ ISA }}WriteMem  : SchedWrite;
def {{ ISA }}WriteJump : SchedWrite;
def {{ ISA }}ReadALU   : SchedRead;
