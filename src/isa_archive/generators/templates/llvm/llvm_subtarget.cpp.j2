// {{ isa_name }} Subtarget — generated by isa-archive

#include "{{ ISA }}Subtarget.h"
#include "{{ ISA }}TargetMachine.h"

#define GET_SUBTARGETINFO_TARGET_DESC
#define GET_SUBTARGETINFO_CTOR
#include "{{ ISA }}GenSubtargetInfo.inc"

using namespace llvm;

{{ ISA }}Subtarget::{{ ISA }}Subtarget(const Triple &TT, StringRef CPU,
                                       StringRef FS,
                                       const {{ ISA }}TargetMachine &TM)
    : {{ ISA }}GenSubtargetInfo(TT, CPU, /*TuneCPU=*/CPU, FS),
      InstrInfo(),
      FrameLowering(),
      TLInfo(TM, *this),
      RegInfo() {}
