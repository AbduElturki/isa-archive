// {{ isa_name }} Subtarget — generated by isa-archive
#pragma once

#include "{{ ISA }}FrameLowering.h"
#include "{{ ISA }}ISelLowering.h"
#include "{{ ISA }}InstrInfo.h"
#include "{{ ISA }}RegisterInfo.h"
#include "llvm/CodeGen/TargetSubtargetInfo.h"
#include <string>

#define GET_SUBTARGETINFO_HEADER
#include "{{ ISA }}GenSubtargetInfo.inc"

namespace llvm {

class {{ ISA }}TargetMachine;

class {{ ISA }}Subtarget : public {{ ISA }}GenSubtargetInfo {
  {{ ISA }}InstrInfo    InstrInfo;
  {{ ISA }}FrameLowering FrameLowering;
  {{ ISA }}TargetLowering TLInfo;
  {{ ISA }}RegisterInfo  RegInfo;

public:
  {{ ISA }}Subtarget(const Triple &TT, StringRef CPU, StringRef FS,
                     const {{ ISA }}TargetMachine &TM);

  void ParseSubtargetFeatures(StringRef CPU, StringRef TuneCPU, StringRef FS);

  const {{ ISA }}InstrInfo    *getInstrInfo()    const override { return &InstrInfo; }
  const {{ ISA }}FrameLowering *getFrameLowering() const override { return &FrameLowering; }
  const {{ ISA }}TargetLowering *getTargetLowering() const override { return &TLInfo; }
  const {{ ISA }}RegisterInfo  *getRegisterInfo() const override { return &RegInfo; }
};

} // namespace llvm
